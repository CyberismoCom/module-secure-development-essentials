selectAll.

result(Card) :-
    link(Card, {{cardKey}}, "ismsa/linkTypes/mitigates").

result(Card) :-
    parent(Card, {{cardKey}}),
    field(Card, "cardType", "base/cardTypes/task").

orderBy("title", "ASC").