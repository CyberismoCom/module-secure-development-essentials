selectAll.

result(Card) :-
    ancestor(Card, {{cardKey}}),
    field(Card, "cardType", "secdeva/cardTypes/securityIssue").

childResult(Card, AffectedCard, "affectedCard") :-
   result(Card),
   link(AffectedCard, Card, "ismsa/linkTypes/presentsRisk").

orderBy("rank", "ASC").
