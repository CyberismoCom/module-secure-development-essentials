{{#if results}}

The threat model includes the following interfaces:

{{#each results}}
=== Element: {{this.title}}

xref:{{this.key}}.adoc[{{this.title}}] has the following interfaces:

{{#each results}}

==== Interface: {{this.title}} [{{this.workflowState}}]

The scoping of security testing for xref:{{this.key}}.adoc[{{this.title}}]:

* Basic input validation testing:  {{this.basicInputValidationTestingRequired}}
* Basic stress testing: {{this.basicStressTestingRequired}}
* Fuzz testing: {{this.fuzzTestingRequired}}
* Network traffic load testing: {{this.networkTrafficLoadTestingRequired}}
* Capacity testing: {{this.capacityTestingRequired}}

*Justification for security testing:*

{{this.[secdeva/fieldTypes/interfaceTestingScopingJustification]}}

{{/each}}

{{/each}}

{{else}}

The threat model does not include any interfaces.

{{/if}}
