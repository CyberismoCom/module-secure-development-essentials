selectAll.

result(result).

secdeva_ancestorOrSelf(Ancestor, Card) :-
     ancestor(Ancestor, Card).

secdeva_ancestorOrSelf(Card, Card) :-
     card(Card).

% ----------------------------------------------------------------------------
% Interfaces and physical connections
% ----------------------------------------------------------------------------

% outbound external dataflow
childResult(result, @concatenate(A, B, Title), "externalDataflows") :-
     link(A, B, "secdeva/linkTypes/dataflow", Title),
     secdeva_ancestorOrSelf(A, {{cardKey}}),
     not secdeva_ancestorOrSelf(B, {{cardKey}}).

field(@concatenate(A, B, Title), "secdeva/fieldTypes/direction", "Outbound") :-
     link(A, B, "secdeva/linkTypes/dataflow", Title),
     secdeva_ancestorOrSelf(A, {{cardKey}}),
     not secdeva_ancestorOrSelf(B, {{cardKey}}).

field(@concatenate(A, B, Title), "secdeva/fieldTypes/title", Title) :-
     link(A, B, "secdeva/linkTypes/dataflow", Title),
     secdeva_ancestorOrSelf(A, {{cardKey}}),
     not secdeva_ancestorOrSelf(B, {{cardKey}}).

field(@concatenate(A, B, Title), "secdeva/fieldTypes/externalTitle", External) :-
     link(A, B, "secdeva/linkTypes/dataflow", Title),
     secdeva_ancestorOrSelf(A, {{cardKey}}),
     not secdeva_ancestorOrSelf(B, {{cardKey}}),
     field(B, "title", External).

field(@concatenate(A, B, Title), "secdeva/fieldTypes/externalCardKey", B) :-
     link(A, B, "secdeva/linkTypes/dataflow", Title),
     secdeva_ancestorOrSelf(A, {{cardKey}}),
     not secdeva_ancestorOrSelf(B, {{cardKey}}).

field(@concatenate(A, B, Title), "secdeva/fieldTypes/internalTitle", Internal) :-
     link(A, B, "secdeva/linkTypes/dataflow", Title),
     secdeva_ancestorOrSelf(A, {{cardKey}}),
     not secdeva_ancestorOrSelf(B, {{cardKey}}),
     field(A, "title", Internal).

field(@concatenate(A, B, Title), "secdeva/fieldTypes/internalCardKey", A) :-
     link(A, B, "secdeva/linkTypes/dataflow", Title),
     secdeva_ancestorOrSelf(A, {{cardKey}}),
     not secdeva_ancestorOrSelf(B, {{cardKey}}).

% inbound external dataflow

childResult(result, @concatenate(A, B, Title), "externalDataflows") :-
     link(A, B, "secdeva/linkTypes/dataflow", Title),
     not secdeva_ancestorOrSelf(A, {{cardKey}}),
     secdeva_ancestorOrSelf(B, {{cardKey}}).

field(@concatenate(A, B, Title), "secdeva/fieldTypes/direction", "Inbound") :-
     link(A, B, "secdeva/linkTypes/dataflow", Title),
     not secdeva_ancestorOrSelf(A, {{cardKey}}),
     secdeva_ancestorOrSelf(B, {{cardKey}}).

field(@concatenate(A, B, Title), "secdeva/fieldTypes/title", Title) :-
     link(A, B, "secdeva/linkTypes/dataflow", Title),
     not secdeva_ancestorOrSelf(A, {{cardKey}}),
     secdeva_ancestorOrSelf(B, {{cardKey}}).

field(@concatenate(A, B, Title), "secdeva/fieldTypes/externalTitle", External) :-
     link(A, B, "secdeva/linkTypes/dataflow", Title),
     not secdeva_ancestorOrSelf(A, {{cardKey}}),
     secdeva_ancestorOrSelf(B, {{cardKey}}),
     field(A, "title", External).

field(@concatenate(A, B, Title), "secdeva/fieldTypes/externalCardKey", A) :-
     link(A, B, "secdeva/linkTypes/dataflow", Title),
     not secdeva_ancestorOrSelf(A, {{cardKey}}),
     secdeva_ancestorOrSelf(B, {{cardKey}}),
     field(B, "title", _).

field(@concatenate(A, B, Title), "secdeva/fieldTypes/internalTitle", Internal) :-
     link(A, B, "secdeva/linkTypes/dataflow", Title),
     not secdeva_ancestorOrSelf(A, {{cardKey}}),
     secdeva_ancestorOrSelf(B, {{cardKey}}),
     field(B, "title", Internal).

field(@concatenate(A, B, Title), "secdeva/fieldTypes/internalCardKey", B) :-
     link(A, B, "secdeva/linkTypes/dataflow", Title),
     not secdeva_ancestorOrSelf(A, {{cardKey}}),
     secdeva_ancestorOrSelf(B, {{cardKey}}).

% Interfaces

childResult(result, Interface, "interfaces") :-
    field(Interface, "cardType", "secdeva/cardTypes/interface"),
    field(Interface, "secdeva/fieldTypes/interfaceType", "logical"),
    ancestor(Interface, {{cardKey}}).

field(Interface, "secdeva/fieldTypes/interfaceElementCardKey", Element) :-
    childResult(result, Interface, "interfaces"),
    parent(Interface, Element).

field(Interface, "secdeva/fieldTypes/interfaceElementTitle", Title) :-
    childResult(result, Interface, "interfaces"),
    parent(Interface, Element),
    field(Element, "title", Title).

% Physical connections

childResult(result, PhysicalConnection, "physicalConnections") :-
    field(PhysicalConnection, "cardType", "secdeva/cardTypes/interface"),
    field(PhysicalConnection, "secdeva/fieldTypes/interfaceType", "physical"),
    ancestor(PhysicalConnection, {{cardKey}}).

field(PhysicalConnection, "secdeva/fieldTypes/physicalConnectionElementCardKey", Element) :-
    childResult(result, PhysicalConnection, "physicalConnections"),
    parent(PhysicalConnection, Element).

field(PhysicalConnection, "secdeva/fieldTypes/physicalConnectionElementTitle", Title) :-
    childResult(result, PhysicalConnection, "physicalConnections"),
    parent(PhysicalConnection, Element),
    field(Element, "title", Title).

% ----------------------------------------------------------------------------
% Related risks
% ----------------------------------------------------------------------------

childResult(result, Risk, "risks") :-
    link({{cardKey}}, Risk, "ismsa/linkTypes/presentsRisk"),
    field(Risk, "cardType", "ismsa/cardTypes/risk").

childObject(result, Register, "riskRegister") :-
    secdeva_closestProject({{cardKey}}, Project),
    field(Register, "base/fieldTypes/identifier", "secdeva_register_risks"),
    secdeva_closestProject(Register, Project).

% ----------------------------------------------------------------------------
% Related security issues
% ----------------------------------------------------------------------------

childResult(result, Issue, "issues") :-
    link({{cardKey}}, Issue, "ismsa/linkTypes/presentsRisk"),
    field(Issue, "cardType", "secdeva/cardTypes/securityIssue").

childObject(result, Register, "securityIssueRegister") :-
    secdeva_closestProject({{cardKey}}, Project),
    field(Register, "base/fieldTypes/identifier", "secdeva_register_security-issues"),
    secdeva_closestProject(Register, Project).

% ----------------------------------------------------------------------------
% Related security requirements
% ----------------------------------------------------------------------------

childResult(result, Requirement, "requirements") :-
    link(Requirement, {{cardKey}}, "secdeva/linkTypes/appliesTo"),
    field(Requirement, "cardType", "secdeva/cardTypes/requirement").

childObject(result, Register, "securityRequirementRegister") :-
    secdeva_closestProject({{cardKey}}, Project),
    field(Register, "base/fieldTypes/identifier", "secdeva_register_security-requirements"),
    secdeva_closestProject(Register, Project).

% ----------------------------------------------------------------------------
% Related products
% ----------------------------------------------------------------------------

childResult(result, Product, "products") :-
    link({{cardKey}}, Product, "secdeva/linkTypes/implementedBy"),
    field(Product, "cardType", "secdeva/cardTypes/product").

childObject(result, Group, "ownGroup") :-
    secdeva_closestProject({{cardKey}}, Project),
    field(Group, "base/fieldTypes/identifier", "secdeva_group_own-products"),
    secdeva_closestProject(Group, Project).

childObject(result, Group, "thirdPartyGroup") :-
    secdeva_closestProject({{cardKey}}, Project),
    field(Group, "base/fieldTypes/identifier", "secdeva_group_third-party-products"),
    secdeva_closestProject(Group, Project).

% ----------------------------------------------------------------------------
% Related decisions
% ----------------------------------------------------------------------------

childResult(result, Decision, "decisions") :-
    link({{cardKey}}, Decision, "base/linkTypes/relatesTo"),
    field(Decision, "cardType", "base/cardTypes/decision").

childResult(result, Decision, "decisions") :-
    link(Decision, {{cardKey}}, "base/linkTypes/relatesTo"),
    field(Decision, "cardType", "base/cardTypes/decision").

childObject(result, Register, "decisionRegister") :-
    secdeva_closestProject({{cardKey}}, Project),
    field(Register, "base/fieldTypes/identifier", "secdeva_register_decisions"),
    secdeva_closestProject(Register, Project).

% ----------------------------------------------------------------------------
% Related design specifications
% ----------------------------------------------------------------------------

childResult(result, Document, "specifications") :-
    link({{cardKey}}, Document, "base/linkTypes/relatesTo"),
    field(Document, "base/fieldTypes/identifier", "secdeva_document_design-specification").

childResult(result, Document, "specifications") :-
    link(Document, {{cardKey}}, "base/linkTypes/relatesTo"),
    field(Document, "base/fieldTypes/identifier", "secdeva_document_design-specification").

childObject(result, Phase, "designPhase") :-
    secdeva_closestProject({{cardKey}}, Project),
    field(Phase, "base/fieldTypes/identifier", "secdeva_phase_design"),
    secdeva_closestProject(Phase, Project).

% ----------------------------------------------------------------------------
% Ordering
% ----------------------------------------------------------------------------

order(2, "externalDataflows", 1, "secdeva/fieldTypes/direction", "ASC").
order(2, "externalDataflows", 2, "secdeva/fieldTypes/internalTitle", "ASC").

order(2, "interfaces", 1, "secdeva/fieldTypes/interfaceElementTitle", "ASC").
order(2, "interfaces", 2, "title", "ASC").

order(2, "physicalConnections", 1, "secdeva/fieldTypes/physicalConnectionElementTitle", "ASC").
order(2, "physicalConnections", 2, "title", "ASC").

order(2, "risks", 1, "rank", "ASC").
order(2, "issues", 1, "rank", "ASC").
order(2, "requirements", 1, "rank", "ASC").
order(2, "products", 1, "title", "ASC").
order(2, "decisions", 1, "rank", "ASC").
order(2, "specifications", 1, "rank", "ASC").
