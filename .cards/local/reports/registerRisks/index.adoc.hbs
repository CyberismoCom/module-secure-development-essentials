This register captures identified security risks for your product.
Create a risk if you identify a _cyber threat_ — something that can cause harm by exploiting one or more vulnerabilities. Risks are assessed by their potential for loss: how likely a threat is to exploit a vulnerability, and how severe the resulting harm would be.

Each risk can lead to security requirements, design decisions, or testing activities. Without documented risks, security work lacks focus and prioritisation.

Risks are typically identified during threat modeling but can emerge at any point in the lifecycle.

[TIP] 
====
If the risk affects a certain element of the threat model or a certain asset, then you can create the risk within the affected card. 
====

Add risks that cannot be created in the threat model or in one of the assets by clicking the following button.

{{#createCards}}
  "template": "secdeva/templates/productRisk2",
  "buttonLabel": "Create a risk"
{{/createCards}}

{{#each results}}
{{#if this.riskCount}}
== Risk overview

Risks closed as duplicate, avoided, transferred or not applicable are not included. Risks included: {{this.riskCount}}.
{{#if this.unassessedCount}}
Risks that have not been assessed yet ({{this.unassessedCount}}) are not shown in the risk matrices.
{{/if}}

// The matrices sit side by side when there is room and wrap when there is not;
// the open block keeps the next widget from joining them.
--
{{#report}}
  "name": "base/reports/riskMatrixWidget",
  "title": "Initial risk levels",
  "legend": "Before risk treatment",
  "likelihood": ["Very Low", "Low", "Moderate", "High", "Very High"],
  "impact": ["Very Low", "Low", "Moderate", "High", "Very High"],
  "levels": [
    {"name": "Very Low", "colour": 1},
    {"name": "Low", "colour": 2},
    {"name": "Moderate", "colour": 3},
    {"name": "High", "colour": 4},
    {"name": "Very High", "colour": 5}
  ],
  "levelGrid": [
    {{#each this.levelGrid}}
    [{{#each this.entries}}{{this.level}}{{#unless @last}}, {{/unless}}{{/each}}]{{#unless @last}},{{/unless}}
    {{/each}}
  ],
  "cells": [
    {{#each this.initialCells}}
    {"likelihood": {{this.likelihood}}, "impact": {{this.impact}}, "count": {{this.count}} }{{#unless @last}},{{/unless}}
    {{/each}}
  ]
{{/report}}

{{#report}}
  "name": "base/reports/riskMatrixWidget",
  "title": "Current risk levels",
  "legend": "Residual, or initial if not yet assessed",
  "likelihood": ["Very Low", "Low", "Moderate", "High", "Very High"],
  "impact": ["Very Low", "Low", "Moderate", "High", "Very High"],
  "levels": [
    {"name": "Very Low", "colour": 1},
    {"name": "Low", "colour": 2},
    {"name": "Moderate", "colour": 3},
    {"name": "High", "colour": 4},
    {"name": "Very High", "colour": 5}
  ],
  "levelGrid": [
    {{#each this.levelGrid}}
    [{{#each this.entries}}{{this.level}}{{#unless @last}}, {{/unless}}{{/each}}]{{#unless @last}},{{/unless}}
    {{/each}}
  ],
  "cells": [
    {{#each this.currentCells}}
    {"likelihood": {{this.likelihood}}, "impact": {{this.impact}}, "count": {{this.count}} }{{#unless @last}},{{/unless}}
    {{/each}}
  ]
{{/report}}
--

{{#report}}
  "name": "base/reports/barWidget",
  "title": "Risks by state",
  "bars": [
    {{#each this.states}}
    {"label": "{{this.label}}", "value": {{this.count}} }{{#unless @last}},{{/unless}}
    {{/each}}
  ]
{{/report}}

{{/if}}
{{/each}}
== Risks

The risks are listed highest current risk level first. The current risk level is the residual risk level if it has been assessed, and otherwise the initial risk level.

{{#report}}
  "name": "secdeva/reports/risks"
{{/report}}