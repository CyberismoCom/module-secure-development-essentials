== Design

The Design phase ensures that security is built into your product architecture from the start. These controls run throughout active development — revisit them whenever the design evolves or new components are introduced.

.*What this phase covers*
[%collapsible]
====
[mermaid]
----
flowchart LR
    subgraph CR[Context and requirements]
      direction LR
      PSC[Document product security context] --> SR[Write security requirements]
    end
    subgraph AT[Architecture and threat analysis]
      direction LR
      TM[Manage threat models] --> HR[Manage high-level risks]
      TM --> MI[Manage interfaces]
    end
    subgraph TC[Technology and configuration]
      direction TB
      TP[Manage 3rd party technologies]
      CM[Configuration management plan]
      CR2[Use cryptography properly]
    end
    CR --> AT
    CR --> TC
    AT --> SC[Ensure secure development in subprojects]
    AT --> SR
----
*Context and requirements*

* *Document product security context* — Define intended use, misuse scenarios, essential functions, and environmental security assumptions.
* *Write security requirements* — Capture and review the security requirements that your product must satisfy.

*Architecture and threat analysis*

* *Manage threat models* — Create and maintain threat models to identify design weaknesses and drive security improvements.
* *Manage high-level risks* — Track broad cybersecurity risks tied to the project and product outcomes.
* *Manage interfaces* — Document exposed interfaces as potential attack surfaces.

*Technology and configuration*

* *Manage 3rd party technologies* — Assess and periodically review external and internal dependencies.
* *Make a configuration management plan* — Define how configuration items are stored, versioned, and protected.
* *Use cryptography properly and effectively* — Justify algorithm and key management choices with a dedicated review.

*Supply chain*

* *Ensure secure development in subprojects* — Verify that suppliers and subprojects follow secure development practices.
====

=== Why it matters

Fixing security weaknesses in design is far less costly than fixing them in production. Threat modeling and security requirements give the team a shared understanding of what needs protection and how — reducing surprises later in verification and operations.

=== Controls

{{#report}}
  "name": "secdeva/reports/phase2",
  "controls": "secdeva_register_design-controls"
{{/report}}

=== Design documentation in the registers

Many design-related aspects are documented in the following registers:

* Processes, interfaces, data stores of the threat model and other assets are in {{#report}}"name":"xref","identifier":"secdeva_register_assets"{{/report}}
* Cybersecurity risks are in {{#report}}"name":"xref","identifier":"secdeva_register_risks"{{/report}}
* Security requirements are in {{#report}}"name":"xref","identifier":"secdeva_register_security-requirements"{{/report}}
* Assessments of 3rd party technologies are in {{#report}}"name":"xref","identifier":"secdeva_register_technology-assessments"{{/report}}.
* Decisions are in {{#report}}"name":"xref","identifier":"secdeva_register_decisions"{{/report}}

=== Architecture and design specifications

You can document architecture or design in a specification here.

TIP: If you need to document architecture or design decisions, see {{#report}}"name":"xref","identifier":"secdeva_register_decisions"{{/report}}.

{{#createCards}}
  "buttonLabel": "Create a specification",
  "template": "secdeva/templates/designSpecification"
{{/createCards}}

{{#report}}
    "name": "secdeva/reports/designSpecifications"
{{/report}}

